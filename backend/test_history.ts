import { PrismaClient } from '@prisma/client'; const prisma = new PrismaClient(); async function fakeHistory() { const snap = await prisma.portfolioHistory.findFirst(); if(snap) { await prisma.portfolioHistory.create({ data: { userId: snap.userId, totalValue: snap.totalValue * 0.95, totalInvested: snap.totalInvested * 0.98, date: new Date(Date.now() - 86400000) }}) } }; fakeHistory().then(() => process.exit(0));
